version: "3.9"
services:
  app:
    build: .
    ports: ["8501:8501"]
    env_file: .env
    command: streamlit run app/streamlit_app.py

  api:
    build: .
    ports: ["8000:8000"]
    env_file: .env
    command: uvicorn app.api.main:app --host 0.0.0.0 --port 8000
